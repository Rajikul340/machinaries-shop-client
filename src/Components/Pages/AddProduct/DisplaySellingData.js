import React from 'react';

const displaysellingData = ({sellingData}) => {
    
    return (
        <div>
            <h1>display data </h1>
        </div>
    );
};

export default displaysellingData;